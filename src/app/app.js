import React from 'react';
import ReactDOM from 'react-dom';
import './app.css';

const App = () => (
  <div id={ 'container' }>
  </div>
);

ReactDOM.render(
  <App />,
  document.getElementById('root')
);
